# Python
__pycache__/
*.pyc
*.pyo

# Native acceleration build artifacts (optional Cython extension; build locally
# with `python setup_accel.py build_ext --inplace`). Source stays; outputs don't.
build/
bidi_fast.c
*.pyd
*.obj
*.exp
*.lib
*.so

# EasyTer runtime / per-machine state (defaults ship in code)
easyter_config.json
easyter_session.json

# Diagnostic logs
*.log
_easyter_stderr.log
_easyter_stdout.log
_easyter_paint_error.log
_raw_debug.log
_size_debug.log

# Local backups / experiments (not part of the release)
EasyTer_before_gridengine.py
EasyTer_cmdrunner_backup.py
easyterpro/

# Internal design/plan docs (not for the public repo)
docs/superpowers/
